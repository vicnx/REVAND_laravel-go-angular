import { Identifiers } from '@angular/compiler';

export interface Product {
    _id: Identifiers;
    Slug: String,
    Name: String;
    Description: String;
    Images: String;
    Price: String;
    AuthorID: String;
}