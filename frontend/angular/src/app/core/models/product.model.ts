import { Identifiers } from '@angular/compiler';

export interface Product {
    _id: Identifiers;
    name: String;
    description: String;
    images: String;
    price: String;
    authorid: String;
}